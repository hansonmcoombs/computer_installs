 #todo check iy sll eotkdworks

General linux notes
# bluetooth microphone does not work well on linux, best practice is to use the webcam mic while at home...


##  install software (light) ##

# set windows key for menu map command: xfce4-popup-whiskermenu
# set shortcut for xflock4 to ctrl+alt+shift+l (to allow pycharm refactor)

# curl
sudo apt install curl

# tailscale
curl -fsSL https://pkgs.tailscale.com/stable/ubuntu/focal.gpg | sudo apt-key add -
curl -fsSL https://pkgs.tailscale.com/stable/ubuntu/focal.list | sudo tee /etc/apt/sources.list.d/tailscale.list
sudo apt-get update
sudo apt-get install tailscale
# ~$ tailscale status to get list of tailscale ip addresses

# notepadqq
sudo apt-get update
sudo apt install notepadqq

# chrome
wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
sudo dpkg -i google-chrome-stable_current_amd64.deb

# miniconda check version here: https://docs.conda.io/en/latest/miniconda.html#linux-installers
wget https://repo.anaconda.com/miniconda/Miniconda3-py39_4.10.3-Linux-x86_64.sh
bash Miniconda3-py39_4.10.3-Linux-x86_64.sh

# 7-zip
sudo apt-get install p7zip-full

# rclone
sudo apt install curl
curl https://rclone.org/install.sh | sudo bash

#tmux
sudo apt install tmux

# bitwarden
sudo snap install bitwarden

# git
sudo apt install git

# remote desktop options
# ssh
sudo apt install openssh-server
sudo ufw allow ssh
# to connect ssh username@ip_address
# client: remmina
sudo snap install remmina
# server
# x2go # todo follow up (did not get working)
sudo apt-get install x2goserver x2goserver-xsession
# rdp # todo follow up on ipv4/6 issue, I think this needs ipv6...
sudo apt install xrdp
sudo systemctl enable --now xrdp
sudo ufw allow from any to any port 3389 proto tcp
sudo adduser xrdp ssl-cert

## install software (medium) ##
# pycharm
sudo snap install pycharm-community --classic
# todo manage preferences here?

# libre office
# pre-installed on libreoffice sudo apt install libreoffice

# screen recording tool
sudo wget -q -O - https://screenrec.com/download/pub.asc | sudo apt-key add -
sudo add-apt-repository 'deb https://screenrec.com/download/ubuntu stable main'
sudo apt update
sudo apt install screenrec

# zoom
sudo apt-get update
sudo snap install zoom-client

# teams (use in browser only with teams screenshare chrome extension


# pycharm
sudo snap install pycharm-community --classic

# spotify
snap install spotify

# strechly
sudo snap install stretchly

# time tracker # todo https://alternativeto.net/software/manictime/?platform=linux???
# look into activitywatch https://docs.activitywatch.net/en/latest/getting-started.html#usage
# this is working pretty well, but does not allow selecting info, tagging and summary of tags 
# I should be able to write something pretty quick for this though and other people would like it!



## install software (heavy) ##

# word??? # todo

# qgis # check version here: https://www.qgis.org/en/site/forusers/alldownloads.html#debian-ubuntu
sudo apt install gnupg software-properties-common
wget -qO - https://qgis.org/downloads/qgis-2021.gpg.key | sudo gpg --no-default-keyring --keyring gnupg-ring:/etc/apt/trusted.gpg.d/qgis-archive.gpg --import
sudo chmod a+r /etc/apt/trusted.gpg.d/qgis-archive.gpg
sudo add-apt-repository "deb https://qgis.org/ubuntu $(lsb_release -c -s) main"
sudo apt update
sudo apt install qgis qgis-plugin-grass

# inkscape
sudo add-apt-repository universe
sudo add-apt-repository ppa:inkscape.dev/stable
sudo apt-get update
sudo apt install inkscape


# configure rclone see admin support


## add to auto start
# copy .desktop file to autostart dir or via the gui session and startup.


